<h1>Events edit</h1>

<form action="{{ path('events_update', { 'id': entity.id, 'district': district }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('events',  { 'id': entity.id, 'district': district }) }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('events_delete', { 'id': entity.id, 'district': district }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
