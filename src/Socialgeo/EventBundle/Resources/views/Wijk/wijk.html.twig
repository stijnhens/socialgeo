{% extends 'EventBundle:Default:index.html.twig'%}

{% block body %}
{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}

{% endif %}


<div class="row-fluid">
    <h1>Gentse Wijken</h1>
</div>
<div class="row-fluid">
    <div class="span9 a-map" id="mapHome"></div>
    <div class="span3 wijken-list">
        <ul>
            {% for item in wijken %}
                <li><a href="{{ path('wijken_show', {'naam': item}) }}">{{ item }}</a></li>
            {% endfor %}
        </ul>
    </div>
</div>

{% endblock %}